<view class="page">
  <view class="hero surface">
    <view class="hero-accent"></view>
    <text class="hero-eyebrow">离职权益第一轮估算工具</text>
    <view class="hero-title">{{product.name}}</view>
    <view class="hero-copy">
      根据入职离职日期、工资基数和解除情形，生成金额区间、计算拆解、法律依据和行动清单。
    </view>
    <button class="primary-button hero-button" bindtap="start">开始估算</button>
    <view class="price-note">{{product.priceNote}}</view>
  </view>

  <view class="feature-list surface">
    <view wx:for="{{features}}" wx:key="title" class="feature-item">
      <view class="feature-mark"></view>
      <view class="feature-content">
        <view class="feature-title">{{item.title}}</view>
        <view class="feature-copy">{{item.description}}</view>
      </view>
    </view>
  </view>

  <view class="notice">
    本工具仅用于第一轮估算，不构成正式法律意见。正式使用前请更新地区数据并完成法律复核。
  </view>
</view>

<disclaimer-bar />
